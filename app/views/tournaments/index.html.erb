<p id="notice"><%= notice %></p>

<h1>Torneos</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tournaments.each do |tournament| %>
      <tr>
        <td><%= tournament.description %></td>
        <td><%= link_to 'Ver', tournament %></td>
        <td><%= link_to 'Editar', edit_tournament_path(tournament) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Torneo', new_tournament_path %>

<br>
